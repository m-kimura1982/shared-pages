# 調剤報酬・薬歴まとめ サイト - Claude 作業ガイド

このファイルは別PCでの作業引き継ぎ用です。Claude Code がこのディレクトリを開いたとき自動的に読み込まれます。

## サイト概要

- **サイト名:** 調剤報酬・薬歴まとめ
- **GitHub Pages URL:** https://m-kimura1982.github.io/shared-pages/
- **リポジトリ:** D:\☆shared-pages\（このフォルダ）
- **目的:** 2026年調剤報酬改定の資料・ツールをスタッフ向けにまとめて公開

## サイト構造（2026-05-01 リニューアル後）

**2026-07-20 からカテゴリ別フォルダ構成。** ルート直下にはハブページ8枚と旧URL救済のリダイレクトスタブだけを置き、個別資料は各フォルダに入れる（ルート72ファイル → 18に整理）。**新規ページは必ず該当フォルダに置く**（ルート直下に置かない）。ハブのURLは不変。

```
index.html            ← ホーム（ヒーロー・二本柱・コンテンツ一覧・最近の更新5件）
updates.html          ← 全更新履歴（2段組フォーマット）
加算まとめ.html        ← ハブ：加算・管理料の恒久ページ（検索窓・正式区分の4カテゴリ。育てる主軸）
  └ 加算まとめ/        … 各加算・管理料の個別ページ17（category: kasan）
checklists.html       ← ハブ：算定チェックリスト集（印刷用。ナビ「チェックリスト」あり）
  └ チェックリスト/    … 個別チェックリスト7（category: checklist）
kaitei2026.html       ← ハブ：2026改定 資料一覧（時限アーカイブ）
  └ 改定資料/          … 疑義解釈まとめ・別表・改定固有の資料19（category: kaitei）
tools.html            ← ハブ：実務ツール集
  └ ツール/            … gigi-search.html、届出判定ツール、別表１（１）など4（category: tools）
knowledge.html        ← ハブ：実務ナレッジ集（改定非依存の実務知識・Q&A）
  └ ナレッジ/          … 変更調剤について.html 等4（category: knowledge）
事務スタッフ向け.html  ← ハブ：読者別の棚（ナビには出さない。トップカード・検索・パンくずで到達）
  └ 事務/              … 改定ポイント整理・改定サマリ2（category: jimu）
yakureki/             ← 薬歴（フォルダ内に入口 index.html がある唯一のカテゴリ）
  ├ index.html        … 薬歴マニュアルの入口
  ├ 基本記載ルール〜ハイリスク7薬効群・文例集の17ページ（2026-08-06 に共通ヘッダーへ統一）
  └ 薬歴の書き方_実例解説集.html … 編集ツール（draft/薬歴記載例_見直し.html）の保管庫JSONを取り込んで実例を追加
404.html               ← 旧URLの救済。GitHub Pages は存在しないURLにこれを返す（後述）

assets/               ← サイト共通の部品（js / css / page-meta / wording-rules）
  └ icons/            … favicon 3種・apple-touch-icon・会社ロゴ.png（2026-08-02 にルートから移設）
  └ data-r86.js       … 薬価データ（選定療養費計算ツールが読む）
tools-internal/       ← 公開しない内部スクリプト・試作（deploy.js / tensu-sim）
files/                ← 厚労省通知などの原本PDF
```

**旧URLの救済は `404.html` に集約**（2026-07-20）。ページを移動・統合したときは、リダイレクト用のスタブHTMLを作らず、404.html 内の `MOVED`（旧ファイル名 → 新パス）に1行足す。疑義解釈の統合ページのようにページ内アンカーの対応が要る場合は `GIGI` の対応表に足す。転送先がないURLのときだけ「ページが見つかりません」の案内を出す。サイトルートは `/shared-pages/` かどうかで自動判定するので、ローカルプレビューでも同じ動きになる。

- 戻る導線は共通ヘッダー（site-header.js）が自動で担う。手動の戻るボタンは置かない。
- index.html のコンテンツカード件数（「N資料収録」）は data-count-from による自動カウント。手動メンテ不要。
- **新規ページは必ず site-header.js の PAGES に登録する**（category＋title）。未登録だとサイト内検索・パンくずに出ない（2026-07-05の構成点検で10ページの漏れが見つかった）。カテゴリは `kaitei / kasan / checklist / tools / knowledge / yakureki / jimu`。**キーはサイトルートからの相対パス**（例 `'加算まとめ/調剤時残薬調整加算.html'`）。フォルダ内のページからは `../assets/site-header.js` で読み込む。
- **新規ページを公開フォルダに追加したら `assets/build-page-meta.js` の PUBLIC_DIRS を確認する**（新しいフォルダを作った場合のみ追記。既存フォルダなら不要）。`assets/build-search-index.js` の PUBLIC_DIRS も同じ内容なので揃える。
- **サイト内検索と `ページ一覧.html` は、ページ名だけでなく各ページの見出しも探す。** 索引は `node assets/build-search-index.js` が `h1 / .card-title / .sub-title / .section-title / .tool-name / .item-heading` を集めて `assets/search-index.js` を作る（/公開 の中で走る）。見出しに出てこない言葉でも引っかけたいときは、そのページの `<head>` に `<meta name="search-keywords" content="湿布,貼付剤,前後7日" />` を足す。入口ページ（ハブ）はスクリプト側の SKIP で索引から外している（中身が他ページ名の並びなので、入れるとどの語でも上位に出て邪魔になる）。
- **新規ページを作ったら search-keywords を必ず書く**（2026-08-24 の利用者目線レビューで、現場語で検索が当たらない問題が見つかったため）。**入れるのは「本文にあるのに見出しに出てこない言葉」**：現場語・略語（地支体・特管2・服情提）、旧名称（医療DX推進体制整備加算）、代表的な薬剤名（ワーファリン・リクシアナ）、患者や事務が使う言い方（ジェネリック・限度額）。推測で書かず、そのページに実際に書いてある内容から拾う。
- **定型見出しは索引に入らない**（「関連する疑義解釈」「関連ページ」「原文を見る」「目次」など。`build-search-index.js` の `SKIP_TERMS`）。どのページにも同じ文字列で出る見出しはページを見分ける手がかりにならず、「疑義解釈」で検索したときに本命が埋もれるため。一方「レセプト摘要欄記載事項」「薬歴記載事項」は**残す**（「吸入 薬歴」のような2語検索で効くので消さない）。
- **draft/ のページはルート相対でリンクを書く慣習**（昇格後にルートへ置く前提）。フォルダへ昇格させるときは相対パスの調整が必要。
- `tools-internal/deploy.js` は新規HTMLをルート直下にコピーする作りのまま。フォルダ構成にした今は、コピー後に該当フォルダへ移動してリンクを直す必要がある（または手動で配置する）。公開は原則 `/公開` を使うので出番はほぼない。
- **アイコン・ロゴは `assets/icons/` に置く。ルート直下には増やさない。** 参照は site-header.js が自動注入するので、新規ページ側で書く必要はない。
- トップは2026-07-20から**「調剤報酬」「薬歴マニュアル」の二本柱構成**（ヒーロー直下に大型入口カード2枚 → 調剤報酬の棚6カード → 薬歴の棚2カード → 更新情報）。ヒーロー文言は「調剤報酬も薬歴も、『現場で使える形』に。」。

## スキル（.claude/commands）

| コマンド | 用途 | いつ使う |
|---|---|---|
| /推敲 | 本文・見出しを文章7ルール＋内容の混在チェックで一括点検（旧→新の対照表で提案・承認制） | ページの文言を棚卸しするとき。※書き下ろし時も7ルールは常時適用（コマンド不要） |
| /デザイン点検 | デザインルール準拠チェック（配色・見出し・共通パーツ） | 新規ページ完成時・大きな改修後 |
| /公開 | 3か所反映＋page-meta再生成＋push の定型手順 | ページを公開・更新するとき（必ずこれ経由） |
| /投稿 | フォーラム投稿の下書き・推敲。private/ の文体資料を必ず読んでから書く | ハローフォーラム向けの文章を作るとき |

## デザインルール（厳守・2026-06-02 刷新版）

> **旧ルール（`--blue-light` / `--sub` / `--bg:#f5f7fa` 等）は廃止。** 新規ページ・既存改修はすべて下記に従う。
> **ページ背景は全ページ `--bg:#f3f4f6` に統一済み**（2026-08-06。ハブ8枚が `#f5f7fa`、kaitei2026 が `#f2f4f8` のまま残っていて、ハブ→個別ページの移動で背景色が変わっていた）。新規ページも必ずこの値にする。
> 刷新済みリファレンス実物：`選定療養_概要2026.html` / `調剤時残薬調整加算.html` / `薬学的有害事象等防止加算.html` / `在宅薬学総合体制加算2026.html` / `在宅薬学総合体制加算2イ_特例摘要欄ガイド.html` / `調剤報酬QA.html`（Q&A形式：結論常時表示＋解説折りたたみのリファレンス。2026-07-12公開）

- フォント: Noto Sans JP（Google Fonts、weight 400/500/700/900）
- フレームワーク不使用 - 純粋な HTML + CSS のみ
- レイアウト: `.page`（max-width:900px）に `.card` を縦積み

### 配色と役割（最重要）

| 色 | 変数 | 役割 |
|----|------|------|
| 青 `#1e5fa8` | `--blue` | メイン。**変化・新規**、H2/H3マーカー、`.em-change` |
| 黄マーカー `#fff59d` | `.marker` | 本文で**読ませたい箇所**のアンダーライン |
| アンバー `#b45309` 系 | `--amber` / `--amber-bg:#fdf0e3` / `--amber-bd:#e8c9a3` | **最終手段。青・グレー・黄マーカーで区別しきれない時だけ使う**（後述） |
| 赤 `#cc0000` | `--red`（定義は残してよい） | **使わない** |
| グレー文字 | `--txt-2:#444444` / `--txt-3:#5e6470` | 補助テキスト。**薄すぎ厳禁**（#999以下は「・」等の弱い記号のみ） |

**役割分担の原則：** 基本は **青（変化・新規／強調）＋黄マーカー（読ませたい）＋補助グレー** の3役だけで構成する。注意喚起や制約も、まずこの3役と罫線・余白・「×」印で表現する。

**アンバーは「最終手段」：** 上記3役＋グレーでは**意味の区別がどうしてもつかない時だけ**使う色。下記をすべて満たす場合に限る。
- 同一画面に**並列する2系統以上**があり、一方が「気をつける側／制約のある側」だと**色で対比しないと伝わらない**（例：残薬調整の「パターン1＝青／パターン2＝アンバー」の対比、box-amber の制約ボックス）。
- **赤の代わりとして単発の注意に使うのは不可**（注意喚起＝赤という発想自体を捨てる）。
- **NG・不可はアンバーで表さない**（注意と混同するため）。中立グレー＋「×」印で表す。
- **現在の新規ページは原則アンバーなしで作成する。** アンバーありの実装例は `調剤時残薬調整加算.html`（パターン対比・box-amber）が唯一のリファレンス。これに該当しない限り `--amber` 系は定義もしない。

### 配色の拡張：カテゴリ色（やむを得ない場合の例外）

**大原則は「色は極力抑える」**（メリハリで勝負・むやみに色を増やさない）。ただし、内容に**並列カテゴリ**があり、**色を使わないと階層・区分が区別できない**ページ（区分イ/ロ/ハ/ニ、分類別の箱など）に限り、**やむを得ず**上記3役とは別レイヤーの「分類用カテゴリ色」を使う。あくまで**例外であって推奨ではない**。色なしの手段（番号・ラベル・罫線・余白）で足りるなら色は足さない。

- 使う場合も彩度は抑える。例（残薬調整ページ）：`--grp-zaitaku:#1e5fa8`（在宅＝青）／`--grp-kakari:#6b5ea8`（かかりつけ＝紫）／`--grp-other:#5c6370`（その他＝グレー）
- カテゴリ色は「区別」のための色。強調（青）の意味とは別レイヤーとして扱い、混同させない。アンバーを併用する場合（残薬調整など）も、カテゴリ色／強調／アンバーを互いに混同させない。
- **「NG・不可」を示すときはアンバー（＝注意）と混同させない。中立グレー＋「×」印で表す**（残薬調整ページのNGケース参照）。
- **並列カテゴリを塗り分けるときは、濃淡ではなく同じ明度の淡色にする。** 濃淡は「量の差」に読めるので、順序があるもの（段階・件数）以外に使うと意味を1つ足してしまう。淡色なら文字は濃い同系色にして、コントラスト比4.5以上を確保する。
- **淡いピンク `#fbe4e0`／淡いグリーン `#e3efe0` を同じ図の中に並べると「気をつける側／問題ない側」の意味になる**（`事務/入力画面で解説_2026改定後の入力方法.html` の週1回判定の図）。良し悪しの区別がない分類でこの2色を**対にして**使わない。片方だけを分類色の1つとして使うのは問題ない（薄緑は下のパレットに入れてある）。
- リファレンス実装：`調剤時残薬調整加算.html`（やむを得ずカテゴリ色を使った例）／`夜間・休日・深夜訪問加算.html`（時間帯の図解。パレットのクリーム・藤・青＋グレー。隣り合う帯を暖寒で割る）
- 例外でカテゴリ色を使う場合も**赤は使わない**は不変。

### 図解の淡色パレット（ページごとに調色しない）

図解で並列カテゴリを塗り分けるときは、下の5色から選ぶ。**ページごとに色を作ると、同じ意味のものがページごとに違う色になって読者が覚えられない。** 明度をほぼ揃えてあるので、どの2色を隣に置いても「量の差」には読めない。文字色は必ずセットの濃色を使う（背景に黒文字を載せない）。

| 色 | 背景 | 文字 | 文字とのコントラスト |
|---|---|---|---|
| クリーム | `#f3e8d0` | `#7a5718` | 5.4 |
| 藤 | `#e5e0f2` | `#4b3f80` | 7.0 |
| 青 | `#dfe9f6` | `#2a5580` | 6.3 |
| 薄緑 | `#e3efe0` | `#3d6b2e` | 5.3 |
| 薄ピンク | `#fbe4e0` | `#8c3a2e` | 6.3 |
| グレー（該当なし用） | `#f2f2f2` | `#444444` | 8.7 |

- **3色までに収める。** 4色以上要るなら、たいてい図の分け方のほうを見直したほうがよい。
- **隣り合う帯は暖色と寒色で割る**（クリーム↔藤／クリーム↔青）。藤と青、薄緑と青は明度も色相も近いので隣に置かない。
- **色だけに意味を持たせない。** どの帯にも名前を書く。色は探しやすくするための補助で、モノクロ印刷や色覚特性があっても読めるようにしておく。
- 変数名は用途で付ける（`--tl-yakan-bg` のように「何を表す色か」）。`--green` のような色名は付けない。

### :root テンプレート

```css
*, *::before, *::after { box-sizing:border-box; margin:0; padding:0; -webkit-tap-highlight-color:transparent; }
:root {
  --text-xs:13px; --text-sm:15px; --text-md:16px; --text-lg:18px; --text-xl:22px;
  --pad-card:22px; --gap-card:16px;
  --blue:#1e5fa8; --red:#cc0000; --ink:#222222;        /* --red は定義のみ・使用禁止 */
  --bg:#f3f4f6; --white:#ffffff;
  --box-blue:#e4f0f8; --box-gray:#f2f2f2;
  --border:#d1d5db; --border-dk:#9ca3af;
  --radius:10px; --radius-sm:6px; --bw:0.5px; --bw-h:1px; --bw-a:3px; --bw-t:4px;
  /* --amber 系は最終手段。原則は定義しない。残薬調整のようなパターン対比が必要な時だけ追加する */
  /* --amber:#b45309; --amber-bg:#fdf0e3; --amber-bd:#e8c9a3; */
  --txt-2:#444444; --txt-3:#5e6470;                    /* 補助テキスト。薄すぎ厳禁 */
}
body { font-family:"Noto Sans JP",sans-serif; background:var(--bg); color:var(--ink); font-size:var(--text-md); line-height:1.8; padding:32px 16px 60px; }
.page { max-width:900px; margin:0 auto; display:flex; flex-direction:column; gap:var(--gap-card); }
```

### 見出し階層

```css
/* H1：装飾なし・大きめ太字＋下にサブ説明 */
.page-title h1 { font-size:var(--text-xl); font-weight:900; letter-spacing:-0.01em; }
.page-title p  { font-size:var(--text-sm); font-weight:500; color:var(--txt-2); margin-top:4px; }
.card { background:var(--white); border:var(--bw) solid var(--border); border-radius:var(--radius); padding:var(--pad-card); }
/* H2：短い青縦棒＋下線。補足語 .qual は弱く、バッジは右寄せ */
.card-title { font-size:var(--text-lg); font-weight:900; border-bottom:var(--bw-h) solid var(--border-dk); padding-bottom:8px; margin-bottom:16px; display:flex; align-items:center; gap:11px; flex-wrap:wrap; }
.card-title::before { content:""; width:4px; height:1.05em; border-radius:2px; background:var(--blue); flex-shrink:0; }
.card-title .qual  { font-size:var(--text-sm); font-weight:700; color:var(--txt-3); }
.card-title .badge { margin-left:auto; }
/* H3：小さな青い四角（.sub-title / .section-title 同形） */
.sub-title { font-weight:900; font-size:var(--text-md); display:flex; align-items:center; gap:9px; margin-bottom:10px; }
.sub-title::before { content:""; width:9px; height:9px; border-radius:2px; background:var(--blue); flex-shrink:0; }
```

### 強調・ボックス・バッジ

```css
.em-change { color:var(--blue); font-weight:900; }                                   /* 変化・新規＝青太字 */
.marker    { background:linear-gradient(transparent 62%, #fff59d 62%); font-weight:700; } /* 読ませたい＝黄マーカー */
/* 変更バッジ：青＝変化／アンバー＝厳格化（注意）。赤は使わない */
.change-badge        { background:var(--box-gray); color:var(--txt-2); font-size:var(--text-xs); font-weight:700; padding:2px 10px; border-radius:4px; }
.change-badge.up,
.change-badge.new    { background:var(--box-blue); color:var(--blue); }
.change-badge.strict { background:var(--amber-bg);  color:var(--amber); }
/* ボックス */
.box-blue  { background:var(--box-blue); border-radius:var(--radius-sm); padding:14px 18px; }
.box-gray  { background:var(--box-gray); border:var(--bw) solid var(--border); border-radius:var(--radius-sm); padding:14px 18px; }
.box-amber { background:var(--amber-bg); border:var(--bw-h) solid var(--amber-bd); border-radius:var(--radius-sm); padding:13px 16px; }  /* 注意・制約 */
.box-amber .ttl { font-weight:900; color:var(--amber); margin-bottom:5px; font-size:var(--text-sm); }
.box-warn  { background:#fff8e1; border:1px solid #e5c200; border-radius:var(--radius-sm); padding:14px 18px; }
/* 箇条書きの「・」はグレーにしてH3の青四角と区別 */
.bullet { display:flex; gap:6px; }
.bullet > span:first-child { flex-shrink:0; color:#999; }
```

※ `@media (max-width:640px)` でフォント・余白を縮小、`@media print` で印刷最適化を付ける（リファレンスページの末尾を踏襲）。

**スマホ幅の縮小率（2026-08-24 に一段大きくした）。** 読者の年齢層に対して本文13pxは小さかったので、資料ページ72枚を14pxに揃えた。新規ページもこの値にする。
```css
@media (max-width:640px) {
  body { font-size:14px; padding:14px 12px 40px; line-height:1.75; }
  :root { --text-xs:11.5px; --text-sm:13.5px; --text-md:14px; --text-lg:16px; --text-xl:18px;
          --pad-card:14px; --gap-card:11px; }
}
```
据え置きにしたページ：印刷1枚もののチェックリスト・図解（体系図・フローチャート）・原文の大きな表（別表Ⅰ／Ⅴ）。詰めて見せることに意味がある。

**表を置いたら必ず `.table-wrap { overflow-x:auto; }` を定義する。** 囲む div だけ書いて CSS の定義を忘れると、表がページごと横に流れてスマホで横スクロールが出る（2026-08-24 に調剤管理料で発見）。

### 番号バッジ（順序＝四角／ケース＝◯で形を分ける）
- 順序のある手順（STEP1→2→3 等）＝**青地・白文字の角丸四角バッジ**（26px / radius 7）。塗りつぶしは引き算流儀の例外だが、順序性を示す手順では正当。
- 既存の**ケース番号は黒丸◯**（`.pat-num`）で使用中。ステップは四角にして役割の衝突を避ける。
- 順序のない強調に塗りバッジは使わない。

## 文章・言葉遣いルール（本文・見出しの推敲基準）

読者は**社内の薬剤師・薬局スタッフ**。厚労省の告示・通知を正確に踏まえつつ、現場で読みやすい自然な日本語にする。ページ作成・改修時は常にこの基準で文言を整える（`/推敲` で一括点検できる）。

**用語・表記の確定基準は2か所に分けて積み重ねる。書き下ろし・改修・/推敲 の際は必ず両方を見る。**

| どこ | 何を置くか | 追跡 |
|---|---|---|
| `assets/wording-rules.json` | 旧→新が一対一で決まる語（機械で検出できるもの） | **される**（2台のPCで同期） |
| `private/推敲対照集.md` | 判断の理由・文体の線引き・見解ブロックの型・見送った理由 | されない（このPCのみ） |

```bash
node assets/check-wording.js                      # 公開ページ全部
node assets/check-wording.js ナレッジ/調剤報酬QA.html   # ファイル指定
node assets/check-wording.js --ng                 # 「要修正」だけ
```

**「要修正」は確定ルール違反なので公開前に0件にする。**「確認」は文脈しだいなので目視で判断する。原文をそのまま載せるページ・現場の質問文・患者へのセリフ例・出典行は対象外。誤検出が出るときはルール側を `skipSpeech` / `okIfPageHas` / `skipFiles` で絞る。

**文章を直したら、その場でルールを足す**（同じ推敲を二度しないための蓄積）。機械で拾える語は wording-rules.json に1行、理由や文脈判断は推敲対照集.md に1行。

1. **硬い・添付文書調をやわらげる。** 添付文書や条文そのままの言い回しは現場向けにかみ砕く（例:「危険作業に従事しないよう説明」は自然に／「条文」→「原文」「通知本文」）。ただし**意味の正確さは崩さない**。
2. **Web制作用語・カタカナ専門語を持ち込まない**（例:「ハブ」「〜ベース」「ペアで整理」などは避け、日本語で言い換える）。
3. **同じ意味の語の重複を避ける**（例:薬歴マニュアル内で「薬歴／記載」を毎回付けない。文脈で自明な語は省く）。
4. **事務的・擬人的な言い回しをやめる**（例:「案内しています」「〜をご参照ください」→ 直接的な言い方に）。ただし**相互参照の定型「〜を参照」「〜も参照」は許容**（サイト全体で定着した簡潔な形。2026-07監査で確定）。
5. **強すぎる断定・算定に踏み込む表現に注意**（例:「初めて算定できます」のような断定は弱める）。ただし**別添3が薬歴に求める必須の記録要素**（「特に指導が必要と判断した理由」等）は、算定に近い言い回しでも**必要な記録なので残す**。
6. **用語は厚労省の正式名を優先。** 現場語（レセコン表示など）と食い違う時は両方あることを一言添える（例:正式は「報告」、現場では「届け出」と呼びがち）。
7. **自社の確立した語り口・編集方針は覆さない。** 外部（GPT等）の推敲提案を取り込む時も、当社の意図を優先して取捨選択する。

**表現の型（`user_content_philosophy` と一致）：** 締めの名言は不要／行動指示は「✓」チェックリスト／具体例は「例）○○」／変化のポイントは「！」で強調／色は極力少なく重要箇所だけ。

**資料ページの本文は常体で統一する。** 「だ・である」「〜する」「〜に注意する」「〜が望ましい」で書き、です・ます調にしない（現行の公開資料ページはほぼこの文体で揃っている）。算定の可否は「算定できる／不可」と断定せず、「算定根拠として十分／不十分」「算定根拠になる」の枠で書く。キャッチーな俗語（「算定の生死を分ける」等）は使わない。
**常体ルールの対象外（です・ますでよい）：** ツールのUI文言・操作説明（計算ツール・判定ツール・疑義解釈検索など）、index / updates の更新告知文、**ハブページのカード説明文**、フォーラム投稿（`/投稿` 参照）。読者への語りかけが必要な場所は資料本文と文体を分ける。

ハブのカード説明文は「〜できます」「〜をまとめています」の敬体で揃える（トップ・加算まとめ・ツール等が元から敬体だったので、2026-08-06に薬歴ハブもこちらへ統一）。本文が常体なのは資料ページの中身だけ。

### 内容の混在チェック（公開前に確認）

7ルール（言葉遣い）に加えて、内容レベルで以下を点検する。

1. **算定要件と社内運用ルールが混ざっていないか。** 通知由来の要件と当社の運用判断は、読者が区別できる形に書き分ける。
2. **「確認したこと」と「指導したこと」が混ざっていないか**（薬歴記載例）。確認 → 結果 → そのうえで指導、の順で書き分ける。
3. **薬剤師向けと事務向けの説明が混ざっていないか。** 読者が違う内容は段落・ページを分ける。
4. **薬歴記載例がテンプレの丸写しに見えないか。** 通則(5)「定型文を用いて画一的に記載するのではなく」。テンプレ集ページには免責ボックスを必ず入れる。

### 摘要欄（レセコメ）ページのルール

レセプト摘要欄の記載を扱うページでは、**コード番号ではなく「レセプト表示文言」を主役**にする。

- 表は「レセプト表示文言」（または「レセプト摘要欄コメント」）を主列に。コード列は原則のせない（レセコンが自動採番・自動付与するので実務で選ぶのは表示文言だから）。どうしても要るなら小さく従に。
- **列見出しに「（これを選択）」等の操作説明を付けない**（2026-07-23）。読みで呼び出して候補から選ぶ手順は現場が分かっているので不要。操作に触れる必要があるときは見出しではなく本文側に「次のいずれかを入力する」と書く。
- 表示文言は別表Ⅰの `c-disp` を一字一致で転記。固定プレフィックス（「該当する状態（…）：」）はグレー寄り、選ぶ可変部は太字・黒。薄すぎ厳禁。
- 加算ページ等で摘要欄が単発のときは、入力する表示文言だけを1行で明快に示す（例：「フォローアップを行った年月日」）。**「入力位置」は書かない**（レセコメは常に摘要欄で自明）。

### 調剤報酬QA（ナレッジ/調剤報酬QA.html）に追記するときの手順

Q&Aが増えたので、2026-07-23から検索窓・カテゴリ目次・NEWチップを載せている。追記時は次の3つを守る。

- **新しい `.qa` には `data-added="YYYY-MM-DD"` を付ける。** 追加から14日間だけ自動でNEWチップが出る（JSが判定。期限管理は不要）。
- **カテゴリを新設したら `<div class="card" id="c-○○" data-keywords="かなよみ">` の形で書く。** id は目次チップのリンク先、data-keywords は検索用のかな読み（漢字はかな検索に引っかからないため。薬剤名がカタカナなら本文で拾えるので不要）。目次チップと件数はカード見出しから自動生成される。
- **Q&Aは算定要件の解説ではない。** 聞かれていない要件を網羅的に足さない（読者は基本を知ったうえで質問してくる）。補足はその質問の答えを分かりやすくするときだけ。

### 原文（告示・通知・施設基準）も各ページに自動生成する（2026-08-31）

**加算ページには原文を書き写さない。** 正本は `改定資料/` の3枚（別表第三＝告示／別添3＝通知／特掲診療料＝施設基準）で、各ページにはマーカーを置いて `assets/build-genbun.js` で生成する。手で転記すると誤字が入るうえ、正本を直したときに片方が古くなる。

```bash
node assets/build-genbun.js          # 生成して書き込む
node assets/build-genbun.js --check  # 差分の確認のみ
```

```html
<!-- 原文（自動生成：node assets/build-genbun.js。マーカーの間は上書きされる） -->
<!-- genbun betten3="kub-10-3-12" hyo3="ku10-3" note="13" kijun="sec-95" -->
<!-- /genbun -->
```

- `betten3` … 別添3 の項目 id（通知）。カンマ区切りで複数可
- `hyo3` … 別表第三 の区分 id（告示）／`note` … その中から載せる注番号（`note="4,5,15"` または `note="all"`）
- `kijun` … 特掲診療料 施設基準の id。付けると見出しが「原文を見る（告示・通知・施設基準）」になる
- `summary` … 折りたたみの見出しを自分で決めたいときだけ
- **id は各ページの「原文参照」リンクの `#…` をそのまま使う。** 実行時に見つからない id・注番号は【確認】に出る
- 注の中の点数表（在宅薬学総合体制加算２のイ・ロ等）は字下げした行として取り込まれる
- **置き場所は「摘要欄 → 原文 → 関連する疑義解釈 → 関連する社内Q&A → 関連ページ」の順**。手書きの補足はマーカーの外に置く

**見た目は `assets/source-refs.css` の1枚に集約してある**（2026-08-31）。折りたたみ（`details.fold` / `.orig`）と最下部の原文参照バー（`.ref-links`）はここだけを直せば全ページに効く。**個別ページに同じ定義を書き足さない。** 使うページの `<head>` に `<link rel="stylesheet" href="../assets/source-refs.css" />` を1行入れる。原文参照バーの書き方は下記で統一（インラインstyleで書かない）。

```html
<div class="ref-links">
  <div class="row">
    <span class="lab">原文参照：</span>
    <a href="../改定資料/別添3_調剤報酬点数表に関する事項2026.html#kub-10-3-11">別添３ › 吸入薬指導加算</a>
  </div>
</div>
```

**資料ページの本文幅は 900px に統一**（2026-08-31に860pxの7枚を揃えた）。図解・大きな表のページだけ 980〜1400px の例外。

### 疑義解釈・社内Q&Aは各ページに一覧を自動生成する（2026-07-23）

**正本は1か所**。疑義解釈は `改定資料/疑義解釈まとめ2026.html`、社内Q&Aは `ナレッジ/調剤報酬QA.html` にだけ全文を置き、各加算ページには「要旨＋結論＋リンク」の一覧を **`assets/build-gigi-list.js` で自動生成**する。加算ページに問答を書き写さない（版が上がって番号が振り直されたとき、片方が古くなって食い違うため）。

```bash
node assets/build-gigi-list.js          # 生成して書き込む
node assets/build-gigi-list.js --check  # 差分の確認のみ
```

**正本に追記したら、対応ページにマーカーを置いてスクリプトを実行する。**

- 疑義解釈まとめに追記 → 該当ページに `<!-- gigi-list:sec-○○ -->` … `<!-- /gigi-list -->`
- 調剤報酬QAに追記 → 該当ページに `<!-- qa-list:c-○○ -->` … `<!-- /qa-list -->`
- `sec-○○` / `c-○○` は正本側のカードの id をそのまま書く
- `<head>` に `<link rel="stylesheet" href="../assets/gigi-list.css" />` が必要（見た目は全ページ共通。調整はこの1ファイルで行う）
- **マーカーの間は毎回まるごと上書きされる。** 手書きの補足・自社の解説は終了マーカーの外に置く
- ブロックは `.page` の中に入れる。疑義解釈と社内Q&Aが両方あるページは「関連する疑義解釈 → 関連する社内Q&A」の順に並べる
- 実行するとブロック未設置のカテゴリが一覧表示される。**新しいページを作ったときはこの一覧を見て、繋げるものがないか確認する**
- **NEWチップは疑義解釈まとめ側の `src-chip new` を引き継ぐ**（加算ページ側で個別に付けない）

## 個別ページの必須要素

### 共通ヘッダー（site-header.js）※手動の戻るボタンは付けない
`<body>` 直後に下記を入れると、共通ヘッダー・パンくず・favicon が自動注入される。**手動の「← トップに戻る」ボタンは置かない**（site-header.js が担うので、付けると二重になる）。
```html
<script src="assets/site-header.js" defer></script>
```

### 最終更新日（自動・記入不要）
個別ページはパンくずの右端に「最終更新 2026年8月20日」が自動で出る（site-header.js が page-meta の日付を読む）。**ページ側に日付を書かない。** 検索やフォーラムのリンクで直接来た人が、いつ時点の内容か判断できるようにするため。ハブは各カードに日付が出ているので対象外。更新から90日を過ぎたページには出さない。

### ページ内目次（長いページのみ）
スマホ幅で**9画面以上**あって、飛び先になる見出しが**5つ以上**あるページには目次を付ける。目次は `assets/page-toc.js` が見出しから自動生成するので、置くのは2行だけ（手で項目を書かない）。
```html
<script src="../assets/page-toc.js" defer></script>          <!-- site-header.js の次の行 -->
<div class="page-toc-wrap"><div id="page-toc"></div></div>   <!-- タイトル直後 -->
```
- 拾う見出しは `.card > .card-title` と `details.fold > summary`。カードを使っていないページは `<section>` + `.section-title` を拾う。
- 目次に出したくないカードは `data-toc="skip"`、目次だけ短くしたいときは `data-toc-label="○○"`（長い見出しはチップが画面幅を超えるので短くする）。
- **タブ等の別のページ内ナビが既にあるページには付けない**（基本記載ルールなど。二重になる）。

### GoatCounter（bodyタグ閉じる直前）
```html
<script data-goatcounter="https://kimura-chozai.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
```

## 変更後の実機確認（完了報告の前に）

**CSS・レイアウト・スティッキー・スクロール挙動を変えたら、PC幅（≥880px）とスマホ幅（<640px）の両方をブラウザで実機確認してから「直しました」と報告する。** 未確認のまま「できました」と言って壊れていた失敗が過去にある。

- `display:none` を足したら、上書きする `display:block` 等が media query 内にあるか必ず確認する。
- アンカーリンク追加時は固定ヘッダーとの被り対策（`scroll-margin-top`）を考慮する。
- 確認できない環境（作業ディレクトリが D:\☆shared-pages でない・ファイル名の全角スペースでプレビュー不可等）なら、その旨を伝えて本人に確認を頼む。延々と設定を試して時間を溶かさない。

## デプロイ

公開・更新は原則 **/公開** を使う（3か所反映 → page-meta再生成 → push まで一括。抜け漏れを構造的に防ぐ）。

**更新・修正を頼まれたら、本文編集だけで終わらせず /公開 の付随作業まで1セットで行う**（UPDATES＝index最新5件・updates.html・NEWバッジ＝page-meta の newUntil・再生成）。毎回個別に指示させない。省くのは本人が「UPDATESに載せないで」「軽微なtypoだけ」と言ったときだけ。更新の告知文は**現場の薬剤師に伝わる言葉で短く1文**にし、「共通ヘッダー」「パンくず」「page-meta」等の内部・技術用語は使わない（何を直したかでなく利用者に何が良くなるかを書く）。

**「短く1文」は index と updates.html の両方に等しくかかる。**（2026-08-25に過去68件を短縮して統一。平均180字→38字）読者は履歴を読みに来るのではなく、**何を更新したかがパッと分かればページ本体を見に行く**。目安は40字前後・1文・敬体で、詳細はリンク先のページにあるので書かない。次の3つは特に入れない。

- 経緯・背景の説明（「〜という訂正があり、これを機に」等）
- 中身の列挙を重ねること（要点1つに絞る）
- 通知の条番号・問番号などの細かい根拠

例外は**【訂正のお知らせ】だけ**。何が誤りだったかの1文に「ご迷惑をおかけしました。」を添える2文にしてよい。
説明文の中にリンクを埋めるのは、**その更新で公開したページがもう1枚あってタイトルからは辿れない場合だけ**にする。

**アップ依頼を受けたら、まず shared-pages と作業フォルダのどちらが更新済みか確認する。** shared-pages に更新済みファイルが直接置かれている場合は、`tools-internal/deploy.js` を使うと古い作業フォルダ版で上書きしてしまう。その場合は deploy.js を使わず git add・commit・push のみ行う。

**画像フォルダ `images（レセコン画像など）/` は `.gitignore` 対象。** 中の画像を新しくページに載せるときは、通常の `git add` では追跡されず**公開後に画像切れ**になる（既存の追跡済み画像40枚超は過去に個別追加されたもの）。**必ず `git add -f "images（レセコン画像など）/…"` で明示追加**してからコミットする。公開ページに画像を追加したら、push後にライブURL（`https://m-kimura1982.github.io/shared-pages/…`）で画像が 200 で返るか確認する（2026-07-13、調剤報酬QA のレセコン画像1枚が add 漏れで画像切れになっていた）。レセコン設定画面のスクショは患者情報を含まないので公開可。

手動でやむを得ずやる場合の最小手順：
```bash
cd "D:/☆shared-pages"
git add <対象ファイル> && git commit -m "メッセージ"
node assets/build-page-meta.js
git add assets/page-meta.json assets/page-meta.js && git commit -m "page-meta 再生成"
git push
```
※ build-page-meta.js は git のコミット日から lastUpdated を読むため、必ず「ページを先にコミット → 再生成 → 再コミット」の順。

**中身を変えないコミットは、メッセージの1行目に `[表示は変えない]` を付ける。** 検索キーワードの追加・共通スクリプトの差し替え・アクセス解析の埋め込みなど、読者から見てページの中身が同じものを更新扱いにすると、パンくず右端の「最終更新」が今日の日付になり、開いた人が何が変わったのか探すことになる。この目印が付いたコミットは build-page-meta.js が最終更新日の計算から外す（20ファイル以上の一括コミットを外すのと同じ仕組み）。更新情報（index・updates）にも載せない。

## git 初回セットアップ（別PCの場合）
```bash
git config --global credential.helper manager
git config --global user.name "m-kimura1982"
git config --global user.email "m-kimura1982@users.noreply.github.com"
git config --global --add safe.directory 'D:/☆shared-pages'
```

**このリポジトリは USB メモリ上にある（このPCでは D: ドライブ）。** USB ごと別PCへ持っていけば private/ も一緒に移動するので、コピーは要らない。ただし**別PCではドライブレターが変わることがある**（E: など）。その場合は上の safe.directory をそのレターで設定し直す。`.claude/launch.json` は相対パスにしてあるのでレターが変わっても動く。

**private/ は git に入らないので、USB が壊れると失われる。** 公開ページは GitHub から戻せるが private/ は戻せないので、時々どこかにコピーを取っておく。

## ローカルプレビュー
```bash
npx serve . -p 3000
# → http://localhost:3000
```

## 公開ルール
- git push したら「チェックしてください」と伝える
- ユーザーが「公開お願いします」と言ったら push する
- `.claude/` の設定ファイル（settings.local.json, launch.json）はコミットしない

## 社内向けの取り決めは private/ に置く

**このリポジトリは public。** 社内固有の運用ルール・個人名・個別指導に関わる話は、コミットされる CLAUDE.md には書かず、`private/社内運用メモ.md`（.gitignore済み）に置く。

`private/社内運用メモ.md` にあるもの：
- 体制・人間が最終確認する範囲
- private/ の内部資料の扱い（フォーラム投稿の文体サンプル等。/投稿 が参照する）
- **個別指導資料の取り扱い（機密）** … 個別指導に関わる資料をAIに渡すとき・公開ページに出すときの丸め方
- モデル使い分けの目安

**作業を始める前に private/社内運用メモ.md を読む。** private/ は git で同期されないので、別PCではフォルダごとコピーして持っていく。

## 薬歴マニュアル プロジェクト（**2026-07-20 公開済み**）

**現状：** `yakureki/` 配下で公開中（17ページ＋css/js）。カテゴリ入口は `yakureki/index.html`（site-header.js の CATEGORIES.yakureki.url も同URL）。トップは「調剤報酬」「薬歴マニュアル」の二本柱構成。**技術基盤（サイトルート相対パスキー・SITE_ROOT自動導出・page-meta の PUBLIC_DIRS）は対応済み**なので、ページ追加は「yakureki/ にファイルを置く → site-header.js の PAGES に `'yakureki/○○.html'` で1行登録」だけでよい。

**公開方針（2026-07-05決定・07-20実施）：** 別サイトに分けず、同一サイトの薬歴カテゴリとして公開。理由：①サイト全体が noindex の社内専用なので分離する必要がない ②相互リンク（算定の正本＝加算まとめ／薬歴の正本＝薬歴マニュアル）を壊さない ③更新フロー・検索・page-meta の二重管理を避ける。改定ページ（kaitei2026）からは導線を作らない。

**薬歴ページも共通ヘッダーに統一済み（2026-08-06）。** 非公開作業時の名残だった独自ヘッダー（back-link）は18ページすべてで撤去した。他のページと同じく `<body>` 直後の `<script src="../assets/site-header.js" defer></script>` 1行だけでよい。**手動の戻るボタンは置かない。**

見送り理由だった「sticky ヘッダーと固定目次の干渉」は、**ヘッダー高さを CSS 変数にして解決した**。site-header.js が `--sn-header-h`（PC 66px／700px以下 57px）を `:root` に定義するので、固定目次やアンカーを持つページはこれを足す。

```css
.sticky-toc { top: calc(var(--sn-header-h, 0px) + 20px); max-height: calc(100vh - var(--sn-header-h, 0px) - 40px); }
[id] { scroll-margin-top: calc(var(--sn-header-h, 0px) + 16px); }   /* yakureki.css に定義済み */
```

**fallback の `0px` は必ず書く**（site-header.js が読めなかったときに従来のレイアウトへ戻すため）。sticky な要素やアンカーを持つ新規ページを作るときも同じ書き方にする。

**draft/yakureki/ に残っている非公開WIP（トップ・yakureki入口から導線を作らない）：**
`循環器_高血圧.html`（投薬時知識シリーズ・公開判断待ち）／`声掛け_降圧剤.html`（解体して素材化予定）／`薬歴記載ルール_実務チェックリスト.html`（未完成・リンク全削除済み）／`_stub_template.html`／`_設計メモ_投薬時知識シリーズ.md`。これらは昇格済みページを `../../yakureki/…` で参照する。

**非公開化の3層構造（draft/ 配下に引き続き適用）：**
1. `robots.txt` で `/draft/` を Disallow
2. 各HTMLの `<head>` に `<meta name="robots" content="noindex,nofollow,noarchive">`
3. 公開ページから `draft/` への導線は作らない（**厳守**）

**ファイル名は日本語で運用**（サブフォルダ隔離なので視認性優先）。入口のみ `index.html`。

**薬歴10項目（ア〜オ）：** 令和8年3月5日保医発0305第6号 別添3（2026年改定）
原典：`別添3_調剤報酬点数表に関する事項2026.html`「薬学管理料 通則(4)(5)(6)」
- ア 基礎情報／イ 処方・調剤内容／ウ 患者情報（(イ)〜(ヘ)）／エ 継続的な薬学的管理及び指導の留意点／オ 薬剤師氏名
- 葉ノード合計10 ＝「10項目」

**重要：** 通則(5)「定型文を用いて画一的に記載するのではなく」→ テンプレ集ページには免責ボックスを必ず入れる。

**個人情報：** 載せない。原資料に誤って含まれていた場合は削除する。

公開済みページの一覧は `git ls-files "*.html"` で確認。
